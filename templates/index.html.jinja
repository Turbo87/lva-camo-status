<!DOCTYPE html>
<html lang="de">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>CAMO Status</title>
    <style>
        body {
            background-color: white;
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif, 'Apple Color Emoji', 'Segoe UI Emoji', 'Segoe UI Symbol';
            margin: 0;
            padding: 0;
        }
        .title {
            margin: 24px 32px;
            font-size: 24px;
        }
        .aircraft {
            display: flex;
            align-items: center;
            padding: 12px 32px;
            border-bottom: 1px solid #eee;
        }
        .aircraft:last-child {
            border-bottom: none;
        }
        .callsign {
            font-weight: bold;
            width: 100px;
        }
        .type {
            flex: 1;
            color: #666;
        }
        .status {
            width: 24px;
            height: 24px;
            border-radius: 50%;
        }
        .status-airworthy {
            background-color: #22c55e;
        }
        .status-grounded {
            background-color: #ef4444;
        }
        .status-prewarning {
            background-color: #eab308;
        }
        .status-unknown, .status-error {
            background-color: #9ca3af;
        }
    </style>
</head>
<body>
    <h1 class="title">CAMO Status LVA</h1>
    {% for aircraft in lva_aircraft %}
    <div class="aircraft">
        <span class="callsign">{{ aircraft.callsign }}</span>
        <span class="type">{{ aircraft.aircraft_type }}</span>
        <div class="status status-{{ aircraft.status }}"></div>
    </div>
    {% endfor %}

    <h1 class="title">CAMO Status Nordstern</h1>
    {% for aircraft in nordstern_aircraft %}
    <div class="aircraft">
        <span class="callsign">{{ aircraft.callsign }}</span>
        <span class="type">{{ aircraft.aircraft_type }}</span>
        <div class="status status-{{ aircraft.status }}"></div>
    </div>
    {% endfor %}
</body>
</html>
